import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Row from 'components/Flexbox/Row';
import Checkbox from 'components/Checkbox';
import Select from 'components/Select';
import Button  from 'components/Button';
import ThemeForm from 'docs/components/ThemeForm';

<Meta title="Documentation/How To Use"/>

## Usage

First import the components you need in your modules from `@pm4ml/react-components` module:

```tsx
import { Row, Button, Checkbox } from '@pm4ml/react-components';
```

then you can just se them as regular React components:

```tsx
function App () {
  return (
    <Row align="left">
      <Button label="Click Me!"/>
    </Row>
  )
}
```

Which will produce the following: 

<Row align="left">
  <Button label="Click Me!" />
</Row>

<br />
<br />

### Look And Feel

You can use the components to build interactive web applications that share the same look and feel.

Consider the following form

<ThemeForm />




